# PhiloFTP 版本发布说明（Changelog）

本文档记录每个正式发布版本的功能变更与 Bug 修复，供用户与协作者快速了解版本差异。

## 版本格式约定

每个版本条目使用以下结构：

```markdown
## [版本号] - 发布日期

### 🆕 新功能
- 描述新增的能力（做了什么、解决什么问题、如何使用）

### ✨ 改进
- 描述对既有功能的优化（改了哪些行为、带来什么体验提升）

### 🐛 Bug 修复
- **描述**：问题现象
- **影响范围**：哪些功能/场景受影响
- **修复方式**：如何修复
```

> 维护约定：每次代码变更后，在「未发布」小节记录变更；发布时把内容迁移到对应版本条目并标注日期。

---

## [1.0.6] - 2026-08-20

### 🆕 新功能
- **文件分享链接**：对任意文件/目录一键生成公开临时链接（`/s/<token>`），无需登录即可访问，适合内网把文件发给访客。支持设置**提取码**（设码后先展示极简提取码输入页校验）与**有效期**（1 小时 / 1 天 / 3 天 / 7 天 / 30 天 / 永久）；目录分享自动打包为 zip 下载，文件支持预览（`?inline=1`）或下载（`?dl=1`）。分享由分享者显式授权，跨用户查找有效且未过期记录，严格限制在分享者 home 内（复用 `safeJoin` 越权防护），不破坏 RBAC。文件行新增「分享」按钮，导航新增「我的分享」视图，可集中查看、复制链接或一键撤销（`DELETE /api/share/:token`）。后端 `internal/handler/share.go` + 公开路由 `/s/:token`，分享记录按用户存于 home 内 `.shares/index.json`。

### ✨ 改进
- **项目目录结构梳理（中等重构）**：按职责重新组织目录，提升可维护性。入口收敛至 `cmd/philoftp/`（package main）；后端按层拆分为 `internal/{config,handler,model,repository,service}`；前端 `web/` 保持独立目录与后端物理解耦；嵌入资源集中于根级 `embedfs.go`（规避 `//go:embed` 不支持 `..` 的限制）。构建入口相应改为 `go build ./cmd/philoftp`，ldflags 注入路径更新为 `github.com/philoftp/cmd/philoftp` 与 `github.com/philoftp/internal/handler`。

### 🐛 Bug 修复
- **分享信息页「下载 / 预览」按钮跳回提取码页**
  - **描述**：访问带提取码的分享、进入文件信息页后，点击「下载文件」或「预览」会跳回提取码输入页，无法执行预期操作。
  - **影响范围**：所有设置了提取码的分享链接（无提取码的分享不受影响）。
  - **修复方式**：根因为信息页下载/预览链接（`?dl=1` / `?inline=1`）未携带 `code` 参数，后端 `publicShareHandler` 判定「未带提取码」而重渲染提取码页。已在 `serveShareInfoPage` 中当分享含提取码时把 `&code=<提取码>` 拼入两个按钮的 `href`，点击后正常返回文件流。

---

## [1.0.5] - 2026-08-19

### ✨ 改进

- **搜索结果行 UI 优化**：移除搜索结果行上的「定位」按钮，改为与普通文件行一致的直接操作。目录行提供「打开」（进入所在目录），文件行提供「预览 / 下载 / 重命名 / 解压 / 删除」，双击文件直接预览，交互更直观。

- **批量操作栏布局重构**：多选后底部操作栏改为「选择控制 + 已选计数」与「操作按钮」两行结构；操作按钮按主次分组（主要「下载选中」、次要「打包下载 / 移动到…」、危险「删除选中」），组间用分隔线区分，层次清晰、间距统一，并适配窄屏（≤760px / ≤480px 响应式）。

### 🐛 Bug 修复

- **搜索结果不显示**
  - **描述**：在文件管理中执行搜索后，搜索结果列表始终为空，无法看到匹配的文件或目录。
  - **影响范围**：文件管理 → 搜索功能（递归文件名搜索），影响所有用户。
  - **修复方式**：定位到前端 `renderSearchResults` 函数第一行调用了未定义的 `updateBatchBar()`，抛出 `ReferenceError` 导致结果渲染被中断。已删除该处无效调用，搜索结果的渲染与后续行操作恢复正常。

- **重命名/新建目录/移动等弹窗点击确定无效**
  - **描述**：输入合法名称后点击「确定」，弹窗不关闭、不执行操作，功能完全无响应。
  - **影响范围**：所有带输入校验的弹窗操作——文件重命名、新建目录、批量移动等。
  - **修复方式**：定位到前端 `promptDialog` 的 `submit()` 校验条件写反（`!opts.validate(v)`），导致输入合法时反而 `return` 不提交。已修正为「校验返回错误时才拦截」，合法输入正常提交并关闭弹窗。

---

<!-- 
历史版本（v1.0.1 - v1.0.4）变更记录：
为保持本文件聚焦未来维护，早期版本的核心变更已记于项目记忆与 Git 提交历史，可按需追溯。
-->
