# 2026-08-15 工作日志

## philoftp 目录分层重构 + 远端推送
- 将扁平的 package main 源码按职责拆分为 model/config/repository/service/handler 内部包，根目录 main.go 组装启动。
- 配置文件移入 configs/，构建产物按平台分入 dist/{macos,windows,linux}/。
- 添加 Makefile（build/run/dist/clean）与 .gitignore。
- 编译 / go vet / 运行验证全部通过（FTP 2121、Web 8080、API 正常）。
- 创建了 GitHub 仓库 https://github.com/PhiloKun/philoftp（gh CLI）。
- dist 二进制过大致 GitHub 408，已 `git rm --cached dist/` 并提交移除（工作区文件保留）。
- 两个提交就绪：53ce344（重构）、6a719a7（移除 dist）。
- 后台执行了 `git push -u origin main`（PID 3647，日志 /tmp/gh_push.log），等待确认成功。
- 后续：用户指示用 keychain 中已有的 Gitee token 同步 Gitee。读取方式 `security find-internet-password -a PhiloKun -s gitee.com -w`（触发系统授权弹窗），账号 PhiloKun。成功创建 Gitee 仓库 https://gitee.com/PhiloKun/philoftp 并推送 main（6a719a7，与 GitHub 一致）。remote 名 gitee。

## Web 界面美化（TDesign 风格，方案2 零依赖）
- 用户选方案2：保持自包含，不依赖任何 CDN。将 `handler/web_ui.go` 的 DashboardHTML 从 TDesign Web Components CDN 方案重写为**纯原生 HTML + TDesign 设计风格 CSS**（零外部请求），确保内网无外网也能完美运行。
- 移除全部 `<t-xxx>` 组件、CDN link/script、MessagePlugin、fallback 逻辑；按钮改为 `.btn`、标签改为 `.tag`、弹窗改为原生 `.overlay/.modal`（点击遮罩关闭）、选择框用原生 `<select>`。
- 功能不变：用户 CRUD、文件浏览/上传/下载(mkdir)/批量上传/搜索、状态卡片、10s 轮询。
- 编译 `go build`/`go vet` 通过。`dd624b1` 已推送 gitee 和 github 两个远端（本轮 GitHub 首次因连接重置失败，重试成功）。

## 新增：登录鉴权 + 注册 + 文件下载进度（c884513）
- 新增 `handler/auth.go`：`AuthManager` 内存会话表 + HTTP-Only cookie（sessionCookie="philoftp_sid"），`Login`/`Logout`/`RequireAuth` 中间件。所有管理/数据 API 与 `/`、`/dashboard` 页面受保护，未登录跳 `/login`。
- 新增 `handler/web_auth_pages.go`：`LoginHTML` 登录页、`RegisterHTML` 注册页（用户名/密码实时校验、密码强度条 4 级、错误反馈、显示密码）。
- 新增 `handler/util.go`：`currentUserOf`(包级 appAuth)、`safeJoin`(防越权穿越)、`checkPasswordStrength`、`PasswordStrengthInfo`、`isASCII`/`urlEncodeUTF8`(中文文件名)。
- `web.go` 改为单端口(WebPort)多分组路由；`downloadHandler` 增强 MIME 推断、中文 `filename*`、Range 断点续传。
- `config` 加 `AllowRegister`(默认 true) + `AllowRegisterEnabled()`；`configs/config.json` 同步加字段。README 已同步登录/注册/下载说明。
- 用户拒绝了 `go run` 启动验证命令，故本轮未做运行验证（前几轮已 run 过且用户曾拒绝启动）。仅做 `go build`/`go vet` 通过。
- 推送：`c884513` 已成功推 gitee；GitHub 因内网 SSL 超时/空响应未成功，待网络恢复后补推。
